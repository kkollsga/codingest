# Docs MDX corpus

Pins the `.mdx` docs-pass arm and the `.txt` rejection.

The uppercase extension on this file is deliberate: `discover_docs` accepts it
case-insensitively, so its `concept_id` must come out as `README`, with no
extension left on the node id.
