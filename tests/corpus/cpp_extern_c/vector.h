#ifndef VECTOR_H
#define VECTOR_H

/* Angle-collision bait: a REAL project file whose name is also spelled as a
   system header inside the extern-"C" region of `utils.h`. */
int vector_len(void);

#endif
