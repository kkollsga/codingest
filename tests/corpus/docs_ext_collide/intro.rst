Introduction
============

An `.rst` doc whose stem collides with nothing, so it survives untouched and
proves the collision scan drops only actual colliders.

Build a plan with :func:`build_retry_plan`.
